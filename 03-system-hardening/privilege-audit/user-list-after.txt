# User Account Audit — AFTER Hardening
# Target: Kali Lab System
# Analyst: Jean Irakiza

# BEFORE: kali user was in 16 groups (overprivileged)
# AFTER: Restricted to essential groups only

kali:x:1000:1000::/home/kali:/usr/bin/zsh
  Groups: kali sudo
  Changes: Removed adm, dialout, cdrom, floppy, audio, dip, video, plugdev, users, netdev, scanner, bluetooth, lpadmin, wireshark, vboxsf, kaboxer
  Justification: Least privilege principle — only sudo retained for admin tasks

root:x:0:0:root:/root:/usr/bin/zsh
  Status: Password rotated, key-based auth enforced
  Note: zsh shell retained for root (Kali default)

# Service accounts hardened:
# - daemon, bin, sys, sync, games, man, lp, mail, news, uucp, proxy
#   All have /usr/sbin/nologin — CORRECT (no interactive shell)
# - www-data, backup, list — verified non-privileged

# Recommendations:
# 1. Enable 2FA for kali sudo access
# 2. Set sudo timeout to 5 minutes
# 3. Enable auditd to track privilege changes
# 4. Disable unused service accounts (games, news, uucp if not needed)
# 5. Review wireshark group — packet capture = root equivalent
